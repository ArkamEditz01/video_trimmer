name: video_trimmer_example
description: "Shadow Cut Editor - CapCut Style Flutter Video Editor"
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  video_trimmer:
    path: ../
  file_picker: ^8.0.0
  ffmpeg_kit_flutter_min_gpl: ^6.0.3
  google_fonts: ^6.1.0
  path_provider: ^2.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
