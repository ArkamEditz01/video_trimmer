name: video_trimmer_example
description: "Shadow Cut Editor"
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  video_trimmer:
    path: ../
  file_picker: ^8.0.0
  path_provider: ^2.1.2
  speech_to_text: ^7.0.0
  get_thumbnail_video: ^0.7.3
  transparent_image: ^2.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
